<!DOCTYPE html>
<html lang="pt-br">
	<head>
		<meta charset="utf-8">
		<title>Aprovar orcamentos!</title>
	</head>
	<body>
		<table>
			<tr>
				<td>Id</td>
				<td>Nome</td>
				<td>Perfil</td>
			</tr>

			<% for (let i=0; i < users.length; i++) {%>

			<tr>
				<td><%= users[i]._id; %></td>
				<td><%= users[i].nomeUsuario; %></td>
				<td><%= users[i].perfil; %></td>
			</tr>

			<% } %>
		
		</table>
	</body>
</html>